services:
  mi-servicio:
    image: arturocabb/testspersonales:controlSmartHome
    restart: unless-stopped
    environment:
      - API_KEYLG=${API_KEYLG}
      - TELEGRAM_URL=${TELEGRAM_URL}
      - chat_id=${chat_id}
    env_file:
      - .env
    volumes:
      # Archivo específico
      - ~/public/SMARTHOME:/app/.smarthome
    ports:
      - 51827:51827
      
      # Carpeta completa
      #- ./data:/app/data
    
      # Archivo con ruta absoluta
      #- /home/usuario/config.ini:/app/config.ini